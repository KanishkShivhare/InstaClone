<% include ./partials/header.ejs %>
<div class="users bg-zinc-900<% include ./partials/header.ejs %>
    <div class="users bg-zinc-900 p-4 min-h-screen">
        <div class="flex py-4 justify-between  text-white">
            <h1 class="">followers</h1>
            <a href="/feed"><i class="ri-close-line"></i></a>
            
        </div>
        <% user.followers.forEach(userfollowers=> { %>
            <a href="/msg/<%= userfollowers.username %>">
                <div>
                    <div class="flex items-center p-2 mb-1 space-x-4 text-white bg-zinc-800 w-full text-white rounded">
                        <img src="/images/uploads/<%= userfollowers.profilepicture %>" alt="User" class="w-12 h-12 rounded-full">
                        <div>
                            <h1 class="">
                                <%= userfollowers.username %>
                            </h1>
                            <h3 class="opacity-30">
                                <%= userfollowers.name %>
                            </h3>
                        </div>
                    </div>
                </div>

            </a>
            <% });%>
    </div>
    <% include ./partials/footer.ejs %>">
    <h1 class="p-4 text-white">followers</h1>
    <%  user.followers.forEach(user=> { %>
        <a href="/msg/<%= user.username %>">
    <div class="flex items-center p-2 mb-1 space-x-4 text-white bg-zinc-800 w-full text-white rounded">
            <img src="/images/uploads/<%= user.profilepicture %>" alt="User" class="w-12 h-12 rounded-full">
            <div>
                <h1 class=""><%= user.username %></h1>
                <h3 class="opacity-30"><%= user.name %></h3>
            </div>
    </div>
        </a>
    <% });%>
</div>
<% include ./partials/footer.ejs %>